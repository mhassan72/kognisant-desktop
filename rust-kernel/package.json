{
  "name": "kognisant-kernel",
  "version": "0.1.0",
  "description": "Native Rust Kernel for Kognisant Core",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "kognisant-kernel",
    "package": {
      "name": "kognisant-kernel"
    }
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi prepublish -t npm",
    "version": "napi version",
    "watch": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
